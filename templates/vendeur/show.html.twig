{% extends 'layouts/master.html.twig' %}

{% block title %}Personne{% endblock %}

{% block content %}
    <h1>Personne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ personne.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ personne.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ personne.password }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ personne.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ personne.adresse }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ personne.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vendeur_index') }}">back to list</a>

    <a href="{{ path('vendeur_edit', {'id': personne.id}) }}">edit</a>

    {{ include('vendeur/_delete_form.html.twig') }}
{% endblock %}
